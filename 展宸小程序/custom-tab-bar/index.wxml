<van-tabbar active="{{ tagactive }}" bind:change="onChange" placeholder>
	<van-tabbar-item wx:for="{{ list }}" wx:key="index" icon="{{ item.icon }}">{{
		item.text}}
	</van-tabbar-item>
</van-tabbar>

